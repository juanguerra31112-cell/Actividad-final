# ─── SERVIDOR ────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development

# ─── BASE DE DATOS PostgreSQL ─────────────────────────────────────────────────
# Cambia estos valores por los de tu instalación local de PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=arenacore
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# ─── JWT (cámbialo por una clave secreta fuerte en producción) ────────────────
JWT_SECRET=arenacore_super_secret_key_2026
JWT_EXPIRES_IN=7d

# ─── FRONTEND (para CORS) ────────────────────────────────────────────────────
CLIENT_URL=http://localhost:3000
